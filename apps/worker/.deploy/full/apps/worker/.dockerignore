# Context is this directory (apps/worker) — see Dockerfile header. Only .deploy/ is ever
# COPYed; everything else here just needs to not slow down "sending build context".
node_modules
dist
.turbo
.env
.env.*
*.log
