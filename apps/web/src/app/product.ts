/**
 * What the product is called.
 *
 * "IEP" was an internal shorthand that leaked into the interface and became the header,
 * the page title and the sidebar heading. An acronym nobody has been taught is not a
 * name — it reads as a placeholder, which is exactly how it read to the people it was
 * demonstrated to.
 *
 * The full name is the default everywhere. The short form survives only where a full
 * name genuinely will not fit, and even then never as the first thing someone sees.
 */
export const PRODUCT_NAME = "Employee Idea Evaluation & Innovation Platform";

/** For a browser tab or a breadcrumb, where the full name would crowd everything else. */
export const PRODUCT_SHORT = "Idea Platform";

export const ORG_NAME = "Sage IT";
