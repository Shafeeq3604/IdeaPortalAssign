# Contract amendment log

Every additive change to a P0-frozen contract gets one line here (SPEC §14.1).
Breaking changes do not appear here — they require a superseding ADR.

| Date | Contract | Change | Kind | Consumers updated |
|---|---|---|---|---|
| 2026-08-26 | — | **P0 baseline frozen**, `@iep/contracts@1.0.0` | baseline | — |
| 2026-08-26 | `ApiEnv` | `ANTHROPIC_API_KEY` relaxed from "must be undefined" to "must be empty or absent" | additive | apps/api. The guard still rejects a real key; a single shared dev `.env` now works. |
| 2026-08-26 | `@iep/contracts` exports | `env.ts` removed from the root barrel; now a separate `@iep/contracts/env` entry point | **corrective** | apps/api. Server-only session/OIDC secret schemas were reachable from the browser bundle. Verified absent from the built client. |
| 2026-08-26 | `@iep/ui` exports | Added `./theme.css` and `./tokens.css` subpath exports | additive | apps/web |
| 2026-08-26 | Custom components | `EmptyState` / `ErrorState` pulled forward from P1 and implemented | additive | Both now *require* a way out in their props, so SPEC §6.3 assertion 3 fails at compile time rather than in review. |
| 2026-08-27 | Design tokens | `--dur-pulse: 1800ms`, plus the `defer` and `pending-pulse` keyframes from SPEC §8.3 | additive | packages/ui. The durations were specified in §8.3 but never declared; two components would otherwise each have invented their own. |
| 2026-08-27 | Custom components | `Stepper`, `Provenance`, `StatusPill`, `EvidenceList` implemented against their P0-frozen signatures | additive | packages/ui, apps/web. No prop added, removed or widened. |
| 2026-08-27 | **OPEN — not amended** | P3 names SSE progress (SPEC §14), but no stream endpoint exists in the frozen `ENDPOINTS` list | **requested** | Not taken. The stepper polls `GET /ideas/{id}/analysis/status` every 2s, which meets §9.3's "within 2s of the job event" using only frozen contracts. Adding `GET /ideas/{id}/analysis/stream` is additive and can be done later without changing the UI's data shape — `AnalysisProgressEvent` and `AcceptedResponse.streamUrl` are already in the contract, unused. |
| 2026-08-27 | `navigation.map.ts` | `matchRouteId(pathname)` — which route a concrete URL belongs to | additive | apps/web. Needed once the placeholder fallback was removed: the catch-all has to tell "this URL is a typo" from "this URL is a real page your role cannot see". |
| 2026-08-28 | `AuthProvider`, `User`, `ENDPOINTS` | Password sign-in: `PasswordAuthProvider`, password columns, and `login` / `createUser` / `updateUser` / `get`+`setIdeaFeedback` endpoints | additive | See ADR-023. `login` is the second public endpoint, allow-listed by name in the contract test. |
| 2026-08-28 | `ENDPOINTS`, `navigation.map.ts`, `AnalysisStep`, AI schemas | **Improvement feature REMOVED.** `listRecommendations`, the `idea.improve` route, §6.2 rows 28–30, the `IMPROVEMENT` analysis step, its prompt, schema, fallback and model route | **removal** | Withdrawn by the product owner: the platform is for posting ideas and reacting to them, not for critiquing them. FR-15 is no longer implemented and requirements.md should say so. §6.2 row numbers 28–30 stay retired rather than renumbered — they are references into the SPEC document. |
| 2026-09-01 | `@iep/ui` theme bridge | Tailwind utilities for tokens that already existed: `accent-050/100/600/700`, `grad-from/via/to`, and the display face as `font-serif` | additive | packages/ui, apps/web. No token defined, renamed or changed — these are `@theme inline` mappings onto P0 values. Also **corrective**: `text-grad-from` was already in use on the dashboard hero's amber button and generated no rule at all, so the label inherited white onto amber at roughly 2:1. It is now the 6.6:1 pair tokens.css documents for that stop. |
| 2026-09-08 | `lifecycle.ts` (`Transition`), `permissions.ts` (`can()`) | New `ownerAllowed` field on the `(*, ARCHIVED)` interrupt transitions: the idea's submitter may now archive their own idea from any `INTERRUPTIBLE` state, independent of the pre-existing REVIEWER/ADMIN power to archive/reject/park/block any idea. `INTERRUPTIBLE` exported from `lifecycle.ts` (was module-private) so `permissions.ts`'s coarse `idea:transition` gate can reuse the same list rather than duplicating it. | additive | apps/api (`present.ts`'s `allowedTransitions` computation extended with the same owner-OR-role logic), apps/web (`IdeaShell.tsx`: a reason-required "Archive this idea" dialog, shown only when the API's own `allowedTransitions` includes `ARCHIVED`). No existing transition's `roles`, `submitterOnly`, or behavior changed — REVIEWER/ADMIN retain unrestricted archive/reject/park/block on ideas that are not their own. `ARCHIVED` has no defined return path in `RESUME_TRANSITIONS`, so this is soft-delete (all versions/evaluations/audit history preserved) but not yet reversible through any UI — the dialog copy says so. |
| 2026-09-09 | `schema.prisma`, `ENDPOINTS`, `PERMISSIONS`/`ROLE_PERMISSIONS`, `navigation.map.ts` (`ROUTES` only) | **AI Discovery Agent (SPC-001)** — a new, standalone chatbot feature. `DiscoveryQuery` model + `DiscoveryStatus` enum (own migration, no FK to any idea/evaluation/ranking table — SPC-13); `createDiscoveryQuery` / `getDiscoveryQuery` / `listDiscoveryQueries` endpoints; `discovery:use` permission granted to every role; `discovery` route added to `ROUTES` but deliberately NOT to `RELATIONSHIPS` (that array is locked to SPEC §6.2's exact 46 rows — assertion 2, "no invented rows" — so this route is linked directly from the app shell header instead of the sidebar, which REQUIREMENTS §20 also fixes at four-plus-three destinations, "nothing else"). | additive | apps/api (new `modules/discovery/routes.ts`, `lib/discovery-queue.ts`, `context.ts`'s `DiscoveryEnqueuer`), apps/worker (new `discovery.ts` job runner + `iep.discovery` queue consumer in `main.ts`), packages/ai (new, separate `discovery.ts` — deliberately NOT a widening of the frozen `AiRequest`/`AnalysisStep` pipeline contract, since `AnalysisStep` is FROZEN at P0), apps/web (new `features/discovery/`, a header icon in `AppShell.tsx`, route in `AppRouter.tsx`). Brainstormed against REQUIREMENTS §31/§32, which places "Automated opportunity discovery" in V3 ("after sufficient usage data") and lists "complex multi-agent AI systems"/"advanced vector infrastructure" under "what not to build initially" — shipped as the smaller of the three brainstormed approaches (single model call, no external search API, no vector/RAG layer) specifically to avoid both. Full spec: `docs/specs/SPC-001-ai-discovery-agent.md`. |
| 2026-09-09 | `IdeaSummary` (`packages/contracts/src/schemas/idea.ts`) | New required `feedback: { up, down, myVote }` field, the same shape `GET /ideas/{id}/feedback` already returned. Fixes a real N+1: every `IdeaCard` on `/ideas` independently called `useFeedback(ideaId)` for its read-only vote counts, turning a page of 20+ ideas into 20+ extra requests on top of the one list call. `toIdeaSummary`/`toIdeaDetail` (apps/api/src/modules/idea/present.ts) now take the batched totals as a parameter, the same pattern already used for `scored` (compositeScore/rank) — a new `feedbackForIdeas()` helper in routes.ts fetches every row's counts and the caller's own vote in two grouped queries regardless of page size. | additive | apps/api (`listIdeas`, `listIdeasByRank`, and all three `toIdeaDetail` call sites now pass batched feedback; `listIdeasByRank` gained a required `userId` parameter), apps/web (`VoteCount` in `features/feedback/VoteButtons.tsx` now takes `up`/`down` as props instead of calling `useFeedback(ideaId)` itself; `IdeaListPage.tsx` reads them off the row it already has). The interactive `VoteButtons` control (an idea's own detail page) is unchanged — it still calls `useFeedback`/`useVote` directly, since that is exactly one request for exactly one idea, not a list. |
| 2026-09-10 | `DiscoveryResultItem` (`packages/contracts/src/schemas/discovery.ts`) | `sources` relaxed from `z.array(z.string()).min(1)` (required, at least one) to `z.array(z.string()).default([])` (optional, may be empty). SPC-20 supersedes SPC-12's "no source → drop the item" rule: the Discovery Agent (SPC-001) now presents original, model-generated ideas rather than cited findings, and a generated idea is never required to carry a source. | additive | packages/ai (`AnthropicDiscoveryProvider.run` no longer filters candidate items by `sources.length`), apps/web (`DiscoveryChatPage.tsx`'s `SourceList` renders nothing, and the "Submit as idea" prefill omits the "Sources:" line, when `sources` is empty). Already-persisted `discoveryReport` rows with a non-empty `sources` array remain valid — the field is now optional, not removed. |
| 2026-09-10 | `DiscoveryResultItem` (`packages/contracts/src/schemas/discovery.ts`) | **SPC-23**: replaced the single flat `summary` field with four structured, optional fields — `problem`, `approach`, `whoItHelps`, `expectedOutcome` — mirroring the idea-submission form's own three required sections (`IdeaForm.tsx`). `summary` stays on the schema, now optional, purely for backward compatibility with rows persisted before this change. | additive | packages/ai (`DISCOVERY_SYSTEM_PROMPT` and `DISCOVERY_OUTPUT_SCHEMA` now ask the model for the four fields per item instead of one paragraph; `StubDiscoveryProvider` updated to match), apps/web (`DiscoveryChatPage.tsx` renders the four labeled sections for a structured item, falling back to the old flat-paragraph layout when they're absent; the "Submit as idea" prefill now carries `problemStatement`/`description`/`expectedUsers`/`expectedOutcome` instead of squeezing everything into `description` — `SubmitIdeaPage.tsx`'s `DiscoveryPrefill` widened to match, deliberately never touching the form's optional "Anything else" fields). Already-persisted `discoveryReport` rows with only `summary` remain valid and render via the legacy branch. |
